<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        body {
            display: flex;
            min-height: 100vh;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            gap: 30px;
        }
        form {
            display: flex;
            flex-direction: column;
            gap: 18px;
        }
        form input, button {
            height: 32px;
            font-size: 100%;
        }
        button {
            background: #000;
            color: blue;
            padding: 7px;
        }
        .error {
            color: red;
            font-size: 12px;
            font-style: italic
        }
    </style>
</head>
<body style="background: #0e0f10;color: rgb(93, 250, 250);">

    <form action="/login" method="POST">
        <h1>Log in</h1>
        <% if (locals.errMsg) { %>
            <span class="error"><%= errMsg[0]  %></span>
        <% } %>
        <div>
            <label for="uname">Username: </label>
            <input type="text" name="username" id="uname" required placeholder="username">
        </div>
        <div>
            <label for="pwd">Password: </label>
            <input type="password" name="password" id="pwd" required placeholder="password">
        </div>
        </div>
        <button type="submit">Log in</button>
    </form>
    <div>
        <div>Not Registered Yet? <a href="/register">Register</a></div><br>
        <button onclick="window.location.href='/'">back home</button><br>
    </div>
</body>
</html>